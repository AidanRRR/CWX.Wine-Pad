export interface IWine {
    id: number,
    title: string | null,
    region: string | null,
    country: string | null,
    year: number | null,
    price: number | null,
    description: string | null,
    type: string | null,
    colorId: number | null
}